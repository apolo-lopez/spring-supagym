Hi... Thanks for visiting this project.

This is a multitenant app, developed on Spring Boot and React Native (Dockerized) and Postgresql DB (It uses multiple schemas to create the tenancy).

It is on the initial stages, will update it ASAP with all the functionality.

Regards
